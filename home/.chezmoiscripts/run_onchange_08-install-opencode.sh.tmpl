#!/usr/bin/env bash
# chezmoi:template:left-delimiter="# {{" right-delimiter="}}"
# {{ if not (index . "installOpencode" | default false) }}
# Skip - OpenCode not enabled
exit 0
# {{ end }}
#
# Install OpenCode (AI coding assistant)
#
# Version: 1.0.2

set -euo pipefail

echo "==> Installing OpenCode..."

# Install via official installer
curl -fsSL https://opencode.ai/install | bash

# Verify installation
if command -v opencode &>/dev/null; then
    echo ""
    echo "✅ OpenCode installed successfully!"
    echo "   Version: $(opencode --version 2>/dev/null || echo 'unknown')"
else
    echo "⚠️  OpenCode may need a shell restart to be available in PATH"
fi
